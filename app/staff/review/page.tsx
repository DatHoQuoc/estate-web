"use client";

import { StaffSidebar } from "@/components/layout/staff-sidebar";
import StaffDashboardPage from "@app/staff/page";

export default function StaffReviewQueuePage() {
  // Currently, the Review Queue functionality is in /staff/page.tsx
  // You might want to move that code here, and replace /staff/page.tsx with a true dashboard.
  return <StaffDashboardPage />;
}
